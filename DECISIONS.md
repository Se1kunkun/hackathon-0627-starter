# 設計判断ログ

> チーム名：（記入してください）

---

## 判断ログの書き方

設計中に「なぜこうしたか」という判断を都度記録してください。
小さな判断でもOKです。審査員はこのログを重視します。

---

## 判断ログ一覧

### [ADR-001] 登録方法をif文で選択する

**日時**：2026-06-27

**状況**：
GitHub OAuth登録を追加したいが、まずは大きく設計を変えず、既存の登録処理の流れを残したまま実装したい。

**決定**：
`RegisterInput.signupMethod` を追加し、`register` メソッドの先頭で `github` かどうかをif文で判定する。`github` の場合はGitHub認可コードから取得したプロフィールとしてメールアドレスと名前を設定し、それ以外は従来のパスワード登録として扱う。GoogleやLINEを追加するときも、まずはこの登録方法選択のif文に分岐を追加する。

**理由**：
最初のアイデアを小さく試せるため。既存のDB保存、確認メール送信、ログ記録の処理をそのまま後続で通せる。

**トレードオフ**：
GoogleやLINEが増えるとif文が増えて見通しが悪くなる可能性がある。ただし、今回はまず最小変更でGitHub登録の入口を作ることを優先する。分岐が増えて読みづらくなったら、OAuthプロバイダーごとの処理を別クラスへ切り出す。

---

### [ADR-002] GitHub OAuth処理を段階ごとのメソッドに分ける

**日時**：2026-06-27

**状況**：
登録方法をif文で選ぶ場所は決まったため、次にGitHubログイン部分の中身を少し具体化したい。

**決定**：
GitHub分岐の中で、認可コードをアクセストークンに交換する `exchangeGitHubAuthorizationCodeForAccessToken` と、アクセストークンからプロフィールを取得する `fetchGitHubProfile` を呼ぶ形にする。GoogleとLINEについては、同じ形で追加できるようにコメントアウトしたテンプレートを残す。

**理由**：
OAuthの処理手順がコード上で読みやすくなり、今後GoogleやLINEを追加するときにどこへ何を足せばよいか分かりやすくなるため。

**トレードオフ**：
現時点ではGitHub APIの実通信はしておらず、モック実装に留める。実運用ではtoken API、user API、emails APIの呼び出しとエラーハンドリングを追加する必要がある。

---

### [ADR-003] パスワード登録も明示的な登録方法として扱う

**日時**：2026-06-27

**状況**：
GitHub以外をすべてパスワード登録として `else` で扱うと、`signupMethod` のタイプミスや未対応プロバイダーが来たときに誤ってパスワード登録として処理されてしまう。

**決定**：
`password` も `if` / `else if` の一つの分岐として明示し、`github` も明示分岐にする。最後の `else` は未対応の登録方法としてエラーにする。

**理由**：
登録方法ごとの責務が明確になり、未知の値を安全に失敗させられるため。将来GoogleやLINEを追加するときも、明示的な `else if` を追加する場所が分かりやすい。

**トレードオフ**：
分岐は少し増えるが、意図しない登録方法がパスワード登録へ流れるリスクを避けられる。

---

### [ADR-004] 未対応の登録方法はelseでエラーにする

**日時**：2026-06-27

**状況**：
登録方法の分岐で最後の `else` に通常処理を置くと、`signupMethod` が想定外の値だった場合でも処理が進んでしまう。たとえば `github` のタイプミスや、まだ実装していない `google` / `line` が渡されたときに、意図せずパスワード登録扱いになる可能性がある。

**決定**：
`password` や `github` など対応済みの登録方法は明示的な `if` / `else if` に置き、最後の `else` では `IllegalArgumentException` を投げる。

**理由**：
未対応の登録方法を安全に失敗させるため。誤った登録方法を早い段階で検知でき、意図しないユーザー作成や後続処理の実行を防げる。

**トレードオフ**：
新しい登録方法を追加するときは、必ず `else if` の分岐を追加する必要がある。ただし、実装漏れがエラーとして表面化するため安全性が高い。

---

### [ADR-005] 登録完了メッセージで後続処理を共通化する

**日時**：2026-06-27

**状況**：
GitHub登録でもパスワード登録と同じウェルカムメール送信とログ記録を必ず通したい。登録方法ごとの分岐の中でメール送信やログ記録を書くと、将来GoogleやLINEを追加したときに処理漏れが起きやすい。

**決定**：
DB保存後に `RegistrationCompletedMessage` を作成し、そのメッセージを `sendWelcomeEmail` と `recordRegistrationLog` に渡す。パスワード登録でもGitHub登録でも、登録方法の分岐を抜けた後に同じ後続処理を呼ぶ。

**理由**：
登録方法に関係なく同じ後続処理を通ることがコード上で分かりやすくなるため。ウェルカムメールの宛先とログ記録に使うメールアドレスも、登録時に確定したユーザー情報から一貫して渡せる。

**トレードオフ**：
小さなメッセージクラスが増える。ただし、後続処理の入力が明確になり、将来メール送信やログ記録を非同期化する場合にも拡張しやすい。

---

### [ADR-006] 登録入力がnullの場合は最初にエラーにする

**日時**：2026-06-27

**状況**：
`register` の最初で `input.getSignupMethod()` を呼ぶため、`input` が `null` の場合は意図しない `NullPointerException` が発生する。

**決定**：
登録処理の先頭で `input == null` を確認し、`IllegalArgumentException` を投げる。

**理由**：
登録方法の分岐に入る前に入力不備を明確なエラーとして返せるため。後続のGitHub登録・パスワード登録・ウェルカムメール・ログ記録の流れにも影響を与えない。

**トレードオフ**：
チェックが1つ増えるが、原因が分かりにくい `NullPointerException` を避けられる。

---

<!-- 判断が増えたらADR-007, ADR-008 ... と追加してください -->
